import 'package:flutter/material.dart';

class AppColors {
  // ============================================================
  // BRAND
  // ============================================================

  static const Color primary = Color(0xFF7A1F3D);
  static const Color primaryDark = Color(0xFF57142C);
  static const Color primarySoft = Color(0xFFF4E4E9);

  static const Color accent = Color(0xFFC9788F);
  static const Color accentSoft = Color(0xFFF8EDF0);

  // ============================================================
  // BACKGROUNDS
  // ============================================================

  static const Color background = Color(0xFFFCF9FA);
  static const Color surface = Colors.white;
  static const Color surfaceSoft = Color(0xFFF8F3F5);

  // ============================================================
  // TEXT
  // ============================================================

  static const Color textPrimary = Color(0xFF241A1E);
  static const Color textSecondary = Color(0xFF766B70);
  static const Color textMuted = Color(0xFFA69A9F);

  // ============================================================
  // STATUS
  // ============================================================

  static const Color success = Color(0xFF3D9B72);
  static const Color successSoft = Color(0xFFE8F5EF);

  static const Color warning = Color(0xFFE5A43A);
  static const Color warningSoft = Color(0xFFFFF4DF);

  static const Color danger = Color(0xFFD9536F);
  static const Color dangerSoft = Color(0xFFFCEAED);

  // ============================================================
  // BMI
  // ============================================================

  static const Color bmiUnderweight = Color(0xFF5B8DEF);
  static const Color bmiNormal = Color(0xFF3D9B72);
  static const Color bmiOverweight = Color(0xFFE5A43A);
  static const Color bmiObesity = Color(0xFFD9536F);

  // ============================================================
  // UI
  // ============================================================

  static const Color border = Color(0xFFE9DDE1);
  static const Color borderLight = Color(0xFFF1E9EC);

  static const Color divider = Color(0xFFF0E7EA);

  // ============================================================
  // SPECIAL
  // ============================================================

  static const Color gold = Color(0xFFD6A84F);
  static const Color streak = Color(0xFFE8753F);
}
